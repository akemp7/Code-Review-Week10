@using Tracking.Models;

<h1>Here are the vendors currently in your list:</h1>
@if (Model.Count == 0)
{
  <p>There are no vendors for you to see!</p>
}
<ul>
  @foreach (Vendor vendor in Model)
  {
    <li>@vendor.VendorName, @vendor.Id</li>
  }
</ul>